/* package main

import (
	"fmt"
	"strings"
)

func main() {
	s := "Hello World"
	fmt.Printf("len(s): %v\n", len(s))
	fmt.Printf("strings.Split(s, \" \"): %v\n", strings.Split(s, " "))
	fmt.Printf("strings.Contains(s, \"hell\"): %v\n", strings.Contains(s, "hell"))
	fmt.Printf("strings.ToLower(s): %v\n", strings.ToLower(s))
	 fmt.Printf("s[3]: %c\n", s[4])
	fmt.Printf("s[3:5]: %v\n", s[3:5])
	fmt.Printf("s[:5]: %v\n", s[:5]) 
	/* var s string = `
	line 1
	line 2
	`
	fmt.Printf("s: %v\n", s+"line3") */

	/* name := "zhang"
	age := "20"
	s := strings.Join([]string{name, age}, ",")
	fmt.Printf("s: %v\n", s) */
	/* 	var buffer bytes.Buffer
	   	buffer.WriteString("tom")
	   	buffer.WriteString(",")
	   	buffer.WriteString("20")
	   	fmt.Printf("buffer.String(): %v\n", buffer.String()) */
	//msg := fmt.Sprintf("name = %s, age = %d", name, age)
	//fmt.Printf("msg: %v\n", msg)
}
 */